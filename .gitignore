# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/
.venv/
venv/
env/

# Trained model artifacts (regenerated by `python -m scripts.train_all`)
models/*.joblib
models/*.pkl
models/*.json
models/*.txt

# Generated processed data
data/processed/*.csv
data/processed/*.parquet

# Reports are regenerated; keep the directory but ignore large binaries by default.
# (We DO commit a few small PNG/MD reports explicitly via `git add -f` for the README.)
reports/*.png
reports/*.csv

# OS
.DS_Store
